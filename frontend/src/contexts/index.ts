/**
 * Contexts barrel export.
 *
 * ThemeContext was removed in Spec 127 (T026/T075).
 * Use `useSettings()` from `@/hooks/useSettings` for theme management.
 * The `data-theme` attribute is set by useSettings automatically.
 *
 * @module contexts
 */

// ThemeContext removed — use useSettings() hook instead (Spec 127)
