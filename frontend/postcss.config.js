// PostCSS config - Tailwind CSS is handled by @tailwindcss/vite in vite.config.ts
// Only additional PostCSS plugins (like autoprefixer) belong here
export default {
  plugins: {
    autoprefixer: {},
  },
};
