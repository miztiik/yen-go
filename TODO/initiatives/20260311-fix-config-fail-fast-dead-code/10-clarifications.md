# Clarifications

**Last Updated**: 2026-03-11

No blocking clarifications. User request was explicit:

> "config not existing is big failure fallback should be loud error and for example log failure & stop the pipeline gracefully, like we do for malformed sgfs"

All access points and dead code paths identified during investigation.
