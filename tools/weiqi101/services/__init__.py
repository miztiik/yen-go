"""Pure-helper services extracted from receiver.py.

Subpackage for code that has no dependency on HTTP request/response or
the receiver's mutable state. Each module here should be importable
without side effects beyond what its top-level functions describe.
"""
